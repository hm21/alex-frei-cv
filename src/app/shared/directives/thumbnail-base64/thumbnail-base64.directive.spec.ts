import { ThumbnailBase64Directive } from './thumbnail-base64.directive';

describe('ThumbnailBase64Directive', () => {
  it('should create an instance', () => {
    const directive = new ThumbnailBase64Directive();
    expect(directive).toBeTruthy();
  });
});
