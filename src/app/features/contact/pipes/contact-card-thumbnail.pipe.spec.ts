import { ContactCardThumbnailPipe } from './contact-card-thumbnail.pipe';

describe('ContactCardThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactCardThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
