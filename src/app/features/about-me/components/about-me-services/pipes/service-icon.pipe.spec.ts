import { ServiceIconPipe } from './service-icon.pipe';

describe('ServiceIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceIconPipe();
    expect(pipe).toBeTruthy();
  });
});
