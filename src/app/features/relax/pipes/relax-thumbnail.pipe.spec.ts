import { RelaxThumbnailPipe } from './relax-thumbnail.pipe';

describe('RelaxThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new RelaxThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
