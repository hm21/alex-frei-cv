import { ProjectThumbnailPipe } from './project-thumbnail.pipe';

describe('ProjectThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
